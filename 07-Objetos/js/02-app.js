const producto = {
    nombre: 'Monitor',
    precio: 300, 
    disponible: true,
}

console.log(producto);

console.log(producto.nombre);

console.log(producto['nombre']);

console.log(producto.nombre);